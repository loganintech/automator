use std::time::{Duration, Instant};

struct Interval {
    d: Duration,
    prev: Instant,
}

impl Interval {
    fn with_duration(d: Duration) -> Self {
        Interval {
            d:
        }
    }
}
